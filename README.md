# Wild-Animals-Dataset-Augmentation
This notebook is meant to be runned to augment the dataset of the Wild Animals sounds prediction.
## Requirements
- To run the notebook type the following commands:
	- ```pip install librosa```
	- ```pip install SoundFile```
